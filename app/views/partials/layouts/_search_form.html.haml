#search
  %button.close{:type => "button"} ×
  = form_tag search_doubts_path, method: 'post',id: 'search_doubt_form', remote: true  do
    = search_field_tag :search_content, 'Escribe aqui el contenido'
    %button.btn.btn-primary{:type => "submit"} Search